SET (src_dir ${CMAKE_CURRENT_SOURCE_DIR})

SET (toy_ionization_simulator_sources
        ${src_dir}/ToyIonizationSimulator.cpp
        ${src_dir}/ToyIonizationSimulator.h
        PARENT_SCOPE
        )